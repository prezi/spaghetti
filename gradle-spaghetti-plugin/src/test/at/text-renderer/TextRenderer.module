/**
 * Text renderer module.
 */
module prezi.graphics.text.render.TextRenderer

interface Renderer {
	string render(prezi.graphics.text.Text text)
}

Renderer createRenderer(string prefix, string suffix)
